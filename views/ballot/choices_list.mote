% choices = ballot.choices

<p>Choices:</p>

% if !choices.empty?
  <ul>
    % choices.each do |choice|
    <li>{{ choice.title }} | Added by: {{ choice.added_by }} | Added at: {{ choice.added_at }}
      % if ballot.status? == "active"
        | <a href="/ballot/{{ ballot.id }}/choices/{{ choice.id }}/remove">Remove choice</a></li>
      % end
    % end
  </ul>
% else
    No choices added yet.
% end
